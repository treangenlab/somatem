# Magnet dependancies
# Specific in readme but no conda env was found in the MAGnet repo
# Env made by PK: 30/Jun/25

# packages mentioned (with version numbers; I omitted versions to use the latest ones)
    # Python 3.9
    # NCBI Datasets v15.27.1
    # Minimap2 v2.24-r1122
    # Samtools v1.15.1
    # Biopython
    # Pandas
    # Ete3 v3.1.2
    # BWA v0.7.17

name: magnet
channels:
    - bioconda
    - conda-forge
dependencies:
    # - python=3.9 # has conflict with biopython; 
        # installed 3.5.5 automatically, magnet.py fails, f-string incompatibility
    # - ncbi-datasets # not found in bioconda/conda-forge
    - minimap2
    - samtools
    - biopython
    - pandas
    - ete3
    - bwa
    