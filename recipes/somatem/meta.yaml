{% set name = "somatem" %}
{% set version = "0.7.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/treangenlab/Somatem/archive/refs/tags/v{{ version }}.tar.gz
  sha256: 4ab197230b4fc16f169719c60891ab8701e8b92aac9f36c838397263684b4eca

build:
  noarch: generic
  number: 0

requirements:
  run:
    - bash
    - nextflow >=25.10.2
    - coreutils

test:
  commands:
    - somatem --help
    - test -f "${PREFIX}/share/{{ name }}-{{ version }}/main.nf"
    - test -f "${PREFIX}/share/{{ name }}-{{ version }}/nextflow.config"
    - test -f "${PREFIX}/bin/{{ name }}"

about:
  home: https://github.com/treangenlab/Somatem
  summary: "Best practices pipeline for long-read metagenomics analysis."
  license: GPL-3.0
  license_file: LICENSE
  dev_url: https://github.com/treangenlab/Somatem
  doc_url: https://github.com/treangenlab/Somatem#readme

extra:
  recipe-maintainers:
    - ppreshant
    - microbemarsh
